[project]
name = "computer-vision"
version = "0.1.0"
description = "Air Draw: An interactive real-time hand gesture drawing canvas using OpenCV and MediaPipe"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mediapipe>=0.10.14",
    "opencv-python>=4.13.0.92",
]
